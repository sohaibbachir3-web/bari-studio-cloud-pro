import Head from 'next/head'
import Navbar from '../components/Navbar'
import Footer from '../components/Footer'
import {useState} from 'react'
export default function Contact(){ const [status,setStatus]=useState(''); async function onSubmit(e){ e.preventDefault(); const data=Object.fromEntries(new FormData(e.target).entries()); setStatus('Sending...'); try{ const r=await fetch('/api/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}); if(r.ok) setStatus('Request sent — we will contact you within 24 hours.'); else setStatus('Failed to send.'); }catch(err){ setStatus('Error sending'); } }
 return (<div><Head><title>Contact - Bari Studio</title></Head><Navbar /><main className='container section'><h1>Start a Project</h1><form onSubmit={onSubmit}><div style={{marginBottom:12}}><input name='name' placeholder='Full name' required/></div><div style={{marginBottom:12}}><input name='email' type='email' placeholder='Email address' required/></div><div style={{marginBottom:12}}><select name='projectType'><option>Website</option><option>Landing Page</option><option>E-commerce</option></select></div><div style={{marginBottom:12}}><input name='budget' placeholder='Estimated budget' /></div><div style={{marginBottom:12}}><textarea name='description' rows='5' placeholder='Describe your project'></textarea></div><button type='submit' className='cta'>Send Request</button></form><p style={{color:'#777',marginTop:12}}>{status}</p></main><Footer /></div>) }